# M-PESA Core Ledger (Next.js + TypeScript)

Starter project scaffold for a financial ledger system using Next.js 14 (App Router), TypeScript, and Prisma.

See the project plan in the repository root for details. Run `npm install` then `npx prisma generate` and `npx prisma db push` to initialize.
